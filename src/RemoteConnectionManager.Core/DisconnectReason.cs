
namespace RemoteConnectionManager.Core
{
    public enum DisconnectReason
    {
        ServerNotFound,
        ConnectionEnded,
        ConnectionTimedOut,
        ConnectionTerminated,
        KickedOut
    }
}
